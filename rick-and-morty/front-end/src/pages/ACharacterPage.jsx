const ACharacterPage = () => {
  /*
    TODO: Use useParams() to grab specific ID of a character 
    from CharacterCard from the URL and render a card or hero text for it
    TODO: Use API call to grab character by ID and render it
    */

  return (
    <>
      <h1>A Character Page</h1>
    </>
  );
};

export default ACharacterPage;
